# =====================================================================
# GNOM-HUB - Agent LLM Routing (Direkt, kein Auto-Routing)
# =====================================================================
# Format: agent_name = provider | model
#
# Alle Agents → OpenRouter FREE (openrouter/free = Auto-Free-Router).
# Fallback: Config.OPENROUTER_FREE_MODELS → Ollama lokal.
# =====================================================================

coderag = openrouter | openrouter/free
securityag = openrouter | openrouter/free
researcherag = openrouter | openrouter/free
generalag = openrouter | openrouter/free
soulag = openrouter | openrouter/free
writerag = openrouter | openrouter/free
editorag = openrouter | openrouter/free
watchdogag = openrouter | openrouter/free
